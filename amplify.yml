version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - cd frontend && npm ci
    build:
      commands:
        - cd $CODEBUILD_SRC_DIR/frontend && npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
